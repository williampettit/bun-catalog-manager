{
  "name": "bun-catalog-manager",
  "version": "0.0.0",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "packageManager": "bun@1.3.8",
  "bin": {
    "catalog": "dist/catalog"
  },
  "scripts": {
    "clean": "rm -rf dist coverage",
    "build": "bun run build.ts",
    "typecheck": "tsgo --noEmit",
    "typecheck:src": "tsgo --noEmit -p tsconfig.src.json",
    "typecheck:test": "tsgo --noEmit -p tsconfig.test.json",
    "typecheck:scripts": "tsgo --noEmit -p tsconfig.scripts.json",
    "lint": "oxlint --type-aware",
    "format": "dprint fmt",
    "format:check": "dprint check",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "check": "bun run typecheck && bun run lint && bun run format:check && bun run test"
  },
  "dependencies": {
    "@effect/cli": "^0.73.2",
    "@effect/platform": "^0.94.3",
    "@effect/platform-bun": "^0.87.1",
    "@effect/printer": "^0.47.0",
    "@effect/printer-ansi": "^0.47.0",
    "@effect/vitest": "^0.27.0",
    "effect": "^3.19.16"
  },
  "devDependencies": {
    "@types/bun": "1.3.8",
    "@typescript/native-preview": "^7.0.0-dev.20260209.1",
    "@vitest/coverage-v8": "3.2.4",
    "dprint": "^0.51.1",
    "oxlint": "^1.43.0",
    "oxlint-tsgolint": "^0.11.5",
    "typescript": "5.9.3"
  }
}
